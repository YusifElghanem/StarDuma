# 📄 ملخص تقني لمشروع إضافة Kodi لـ StarDima (Handover Brief)

## 1) نظرة عامة (Project Overview)

المشروع عبارة عن إضافة (Addon) لتطبيق **Kodi** مبنية على **Python 3** (Kodi 21)، وهدفها سحب وعرض محتوى موقع **StarDima**.

الإضافة تدعم حالياً مسارين للموقع:
- المسار القديم (Subdomain-based).
- المسار الجديد المبني على قوالب **DooPlay**.

---

## 2) الهيكل التقني (Technical Stack)

- **اللغة:** Python 3.
- **المكتبات الأساسية:**
  - `requests` لجلب الصفحات والبيانات.
  - `re` (Regex) لاستخراج الروابط والمحتوى النصي.
  - `base64` لفك ترميز روابط التحويل.
- **ملاحظة تصميمية:** تم استبدال `BeautifulSoup` بـ Regex لتقليل استهلاك الذاكرة وتحسين الثبات على الأجهزة الضعيفة.

---

## 3) منطق التنقل ومسار البيانات (Navigation Logic)

الوصول للحلقات يعتمد على 3 محطات:

1. **المحطة 1 (الشبكة / Listing):**
   - جلب قائمة المسلسلات.
   - استخراج الـ `slug` الفريد لكل مسلسل.

2. **المحطة 2 (صفحة المعلومات / Details):**
   - الدخول لصفحة المسلسل.
   - استخراج رابط زر التشغيل الأولي (عادةً يحمل معرف الحلقة الأولى).

3. **المحطة 3 (صفحة المشغل / Player + Slider):**
   - الصفحة الأساسية لاستخراج قائمة الحلقات.
   - المصدر الرئيسي للحلقات يكون عادةً من القائمة الجانبية `#episodes-list-container`.

---

## 4) المشاكل التقنية التي تم حلها (Challenges & Solutions)

### 4.1 مشكلة روابط `undefined`
- **المشكلة:** روابط حلقات السلايدر تأتي أحياناً بصيغة مكسورة مثل:
  - `/tvshow/undefined/play/ID`
- **الحل:** إعادة بناء الرابط برمجياً باستخدام `slug` الصحيح القادم من المحطة الأولى:
  - `tvshow/{slug}/play/{ep_id}`

### 4.2 نظام التحويل والترميز (Redirection + Base64)
- **المكتشف:** بعض الروابط تمر عبر وسيط مثل `gamezon.top`.
- **التفصيل:** الرابط الحقيقي يكون داخل باراميتر `redirect` مشفر Base64.
- **الحل:** فك التشفير باستخدام `base64.b64decode` واستخراج رابط المشغل النهائي.

### 4.3 مشكلة 403 Forbidden في سيرفرات الفيديو
- **المكتشف:** بعض السيرفرات (مثل Hyperwatching) تمنع التشغيل خارج سياق الموقع.
- **الحل:** إرسال Headers إلزامية أثناء طلبات الفيديو:
  - `Referer` مطابق.
  - `User-Agent` قريب من متصفح حقيقي.

### 4.4 تحسين الأداء واستهلاك الذاكرة
- **الحل:** تقليل أحجام الصور (TMDB) من `w500` إلى `w185` أثناء السحب لتقليل الحمل، تسريع الواجهة، وتخفيف الـ Lag.

---

## 5) ملاحظات مهمة للمطور القادم (Developer Notes)

1. **API خفي للحلقات:**
   - بعض الصفحات لا تعرض كل شيء في HTML مباشرة.
   - قد يتم جلب الحلقات من طلب JSON مثل: `/series/season/ID`.

2. **دعم المواسم (Seasons):**
   - توجد قائمة منسدلة `season-item`.
   - يجب تمرير `season_id` بين التنقلات بشكل صحيح.

3. **أولوية التطوير مستقبلاً:**
   - قالب **DooPlay** على `watch.stardima` أسهل وأثبت من المسار القديم.
   - يُفضَّل توجيه معظم التحسينات المستقبلية إليه.

---

## 6) روابط مرجعية للاختبار (Testing URLs)

- **قائمة المسلسلات:**
  - `https://watch.stardima.com/watch/tvshows/`
- **صفحة مسلسل:**
  - `https://watch.stardima.com/watch/tvshows/[slug]/`
- **صفحة مشغل/حلقة:**
  - `https://watch.stardima.com/watch/episodes/[slug]-1-1/`

---

## 7) إضافات مقترحة قبل التسليم النهائي

لرفع جاهزية المشروع أكثر، يُنصح بإضافة النقاط التالية في التسليم النهائي:

- **Known Limitations**: قائمة مختصرة بالحالات التي قد تفشل (تغير HTML/Cloudflare/حظر جغرافي).
- **Troubleshooting سريع**: جدول "العَرَض → السبب المحتمل → الحل".
- **نسخة البيئة المجربة**: إصدار Kodi، إصدار Python الفعلي داخل Kodi، وأي إعدادات شبكة مطلوبة.
- **خطة مراقبة التغييرات**: ما العناصر الحساسة في DOM التي قد تتغير وتكسر السحب.
- **Checklist إصدار**: خطوات تحقق قبل كل release (قائمة، تفاصيل، تشغيل، استخراج رابط مباشر).

بهذا المستند يستطيع أي مطور استلام العمل بسرعة وبسياق تقني واضح.
